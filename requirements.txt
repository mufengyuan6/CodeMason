fastapi==0.111.0
uvicorn==0.30.0
pydantic==2.7.0
tree-sitter==0.22.0
tree-sitter-python==0.21.0
tree-sitter-javascript==0.21.0
tree-sitter-go==0.21.0
pytest==8.2.0
httpx==0.27.0
# v1.27 多模态：OCR 可选依赖（OcrTool 需安装才启用；未装 fail-closed 报错）
# paddleocr 依赖重（~500MB），默认不装——需要 OCR 读字时：pip install paddleocr paddlepaddle
# 轻量化模型自动下载约 15-30MB；英文场景可降 Tesseract
